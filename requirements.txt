# DEPRECATED: see pyproject.toml (PEP 621). Retained for pip compatibility.
# Install via:  pip install -e .      (or)    pip install -r requirements.txt
#
# Optional extras (pyproject.toml):
#   pip install -e ".[news]"     # Phase 4 FinBERT
#   pip install -e ".[regime]"   # Phase 5C hmmlearn
#   pip install -e ".[dev]"      # pytest / ruff / mypy

# Core Dependencies
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
yfinance>=0.2.30
python-dotenv>=1.0.0

# Data Sources
pandas-datareader>=0.10.0    # FRED (core + extended)
requests>=2.31.0             # BLS direct API, BEA direct API
pyarrow>=14.0.0              # parquet cache for BLS/BEA/news data

# Statistical/Econometric
statsmodels>=0.14.0
arch>=6.0.0
scipy>=1.11.0

# Machine Learning
scikit-learn>=1.3.0
xgboost>=2.0.0

# Deep Learning
torch>=2.0.0
# transformers>=4.40.0       # Phase 4: FinBERT news sentiment (install when needed)

# Jupyter
jupyter>=1.0.0
notebook>=7.0.0
tqdm>=4.65.0
